import React from 'react';

class TransactionComponent extends React.Component {
  render(){
    return(
      <div>
        Component used to extract Transaction´s info from database
      </div>
    );
  }
}

export default TransactionComponent;
